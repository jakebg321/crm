'use client';

import { useState } from 'react';
import { useSession } from 'next-auth/react';

export default function RoleSwitcher() {
  // Always return null - component completely disabled
  return null;
} 